# 不提交密钥与本地数据
.env
data/

# 调试/临时脚本与产物（不提交，部分含凭据）
/*.py
/*.js
/*.html
/*.json
__pycache__/
*.pyc
fix_arr_auth.sh

# *arr/qB 运行时 bind 配置（含 API key，禁止提交）
*-config/

# 出网运维脚本：必须随仓库分发，否则重新部署后丢失会导致出网全断
!/clash_watchdog.py
!/restore_vm_egress.sh
